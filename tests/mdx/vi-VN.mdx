---
title: "Hướng dẫn triển khai"
description: "Xử lý thay thế văn bản MDX mà không cần tác động đến JSX, biểu thức hoặc import."
---

import { Callout } from "nextra/components";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

# Hướng dẫn triển khai

<Callout type="warning">
  Keep `projectId="docs"` unchanged when you update this page.
</Callout>

<Tabs items={["cli", "api"]}>
  <Tab value="cli">
    Run `hyperlocalise status --verbose` before publishing the docs portal.
  </Tab>
  <Tab value="api">
    API users should keep the token name `HYPERLOCALISE_API_KEY`.
  </Tab>
</Tabs>

<Card title="Replacement rules" href="/docs/rules">
  Replace the sentence, not the `title` prop or `{locale.toUpperCase()}` expression.
</Card>

<Card
  title="Nested checks"
  href="/docs/nested(checks)"
  icon={<Badge text="beta" />}
>
  <Callout type="info">
    Keep [reference links][mdx-ref] stable and preserve literal text like `MDPH_0_END`.
  </Callout>

  - Mục tiêu lồng đầu tiên với `hyperlocalise sync pull`
  - Second nested item with <Badge text="inline" /> markers
</Card>

Fallback route: {locale === "vi-VN" ? "/vi-VN" : "/"} is computed at runtime.

Use <Badge text="stable" /> builds when the release branch is frozen.

> Đoạn trích dẫn có thể bao gồm <Badge text="quoted" /> và `inlineCode()`.
>
> ![Hình ảnh biểu đồ: Quoted diagram](https://example.com/assets/quoted(flow).png)

<Steps>
  <Step title="Prepare">
    Review the [CLI guide](https://example.com/docs/cli(reference)) before publishing.
  </Step>
  <Step
    title="Publish"
    icon={<Badge text="go" />}
  >
    Ship the docs page and keep `docs/[locale]/index.mdx` untouched.
  </Step>
</Steps>

> Repeat this warning: preserve `href="/docs/rules"` exactly.
> Lặp lại cảnh báo này: giữ nguyên `href="/docs/rules"` một cách chính xác.

```ts
export const projectId = "docs";
```

[mdx-ref]: https://example.com/docs/mdx(reference)
