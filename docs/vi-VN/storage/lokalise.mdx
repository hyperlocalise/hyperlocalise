---
title: "Bộ chuyển đổi Lokalise"
description: "Cấu hình Lokalise API v2 cho đồng bộ kéo/đẩy."
---

## Cấu hình bắt buộc

```jsonc
{
  "storage": {
    "adapter": "lokalise",
    "config": {
      "projectID": "your-project-id",
      "apiTokenEnv": "LOKALISE_API_TOKEN"
    }
  }
}
```

## Các trường cấu hình tùy chọn

- `targetLanguages`
- `timeoutSeconds`

## Phân giải token

- biến môi trường mặc định: `LOKALISE_API_TOKEN`
- ghi đè bằng `apiTokenEnv`

## Các sự cố thường gặp

- lỗi không được phép: thiếu token hoặc phạm vi không đủ
- không khớp locale: căn chỉnh mã ngôn ngữ Lokalise với các locale đích
- lỗi hết thời gian chờ: tăng `timeoutSeconds` cho các dự án lớn
