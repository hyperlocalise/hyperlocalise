---
title: "Mẫu đường dẫn"
description: "Sử dụng các trường thay thế nguồn và đích một cách an toàn trong bản đồ file bucket."
---

## Các biến được hỗ trợ

- `{{source}}`: source locale
- `{{target}}`: target locale
- `{{localeDir}}`: empty for source locale output, otherwise target locale

## Ví dụ

### Tệp địa phương JSON

```jsonc
{
  "from": "packages/ui/lang/{{source}}.json",
  "to": "apps/web/lang/{{target}}.json"
}
```

### Locale template with `[locale]` token

```jsonc
{
  "from": "lang/en.json",
  "to": "lang/[locale].json"
}
```


### Sử dụng Mintlify với mapping đệ quy và glob

```jsonc
{
  "from": "docs/**/*.mdx",
  "to": "docs/{{localeDir}}/**/*.mdx"
}
```

Điều này giúp duy trì cấu trúc thư mục của tệp nguồn và ghi các tệp đã dịch vào các đường dẫn tương đối giống nhau trong mỗi thư mục ngôn ngữ.

With `source=en` and targets `fr`, `es`, and `zh`:

- `target=en` resolves `docs/{{localeDir}}/index.mdx` to `docs/index.mdx`
- `target=fr` resolves `docs/{{localeDir}}/index.mdx` to `docs/fr/index.mdx`
- `target=es` resolves `docs/{{localeDir}}/index.mdx` to `docs/es/index.mdx`
- `target=zh` resolves `docs/{{localeDir}}/index.mdx` to `docs/zh/index.mdx`

Use this pattern when your English source pages live directly under `docs/` and translated pages should be written into locale folders.

## Các mẫu tốt

- Giữ cây nguồn và cây đích riêng biệt.
- Use stable bucket names by domain (`ui`, `marketing`, `emails`).
- Giữ một trách nhiệm duy nhất cho mỗi bản ánh xạ.

## Các lỗi phổ biến

- Ghi HLMDPH_71AA225F938B_0 vào các đường dẫn trở lại thư mục nguồn.
- Sử dụng cùng một đường dẫn đầu ra cho nhiều thư viện lưu trữ.
- Trộn các token ngôn ngữ địa phương với việc thay thế chuỗi tùy chỉnh bên ngoài các mẫu.

## Kiểm tra nhanh

Trước khi thực hiện:

1. xác nhận HLMDPH_BA081439DFC6_0 giải quyết cho khu vực ngôn ngữ nguồn,
2. xác nhận HLMDPH_71AA225F938B_0 có thể được tạo cho mỗi định cấu địa phương đích,
3. chạy HLMDPH_EC005404C47A_0 để kiểm tra các nhiệm vụ đã lên kế hoạch.
