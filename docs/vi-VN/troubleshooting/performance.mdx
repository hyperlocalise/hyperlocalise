---
title: "Hiệu năng"
description: "Cải thiện hiệu suất và độ ổn định trong các dự án dịch thuật lớn."
---

## Giảm thiểu công việc không cần thiết

- dựa vào việc bỏ qua tệp khóa trong HLMDPH_027AA2E0049A_0
- chia sẻ các bản ánh xạ rất lớn thành các nhóm nhỏ hơn
- limit locale scope with repeated `--locale`

## Cải thiện độ tin cậy của quá trình đồng bộ

- set adapter `timeoutSeconds` when working with large projects
- chạy các tác vụ đồng bộ theo từng khu vực thay vì đồng bộ toàn bộ khi cần thiết
- sử dụng định dạng JSON để dễ dàng triển khai logic thử lại tự động

## Xác định các điểm nghẽn

- compare `planned_total` vs `executable_total`
- inspect repeated failures by `target` and `key`
- kiểm tra giới hạn tốc độ của mạng/API từ nhà cung cấp của bạn
