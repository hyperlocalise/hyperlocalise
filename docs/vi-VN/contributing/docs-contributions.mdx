---
title: "Đóng góp tài liệu"
description: "Cách đóng góp cải tiến cho trang tài liệu này."
---

## Chỉnh sửa trên GitHub

1. mở trang,
2. nhấp chỉnh sửa,
3. gửi một pull request.

## Quy trình tài liệu cục bộ

1. nhân bản kho lưu trữ,
2. cài đặt Mintlify CLI (`npm i -g mint`),
3. chạy `mint dev` trong `docs/`,
4. xác thực liên kết với `mint broken-links`,
5. mở pull request.

## Tiêu chuẩn viết

- sử dụng thể chủ động và ngôi thứ hai,
- giữ các ví dụ có thể thực thi,
- dẫn đầu mỗi trang bằng mục tiêu và các điều kiện tiên quyết,
- giữ các cờ lệnh căn chỉnh với đầu ra trợ giúp CLI hiện tại.
