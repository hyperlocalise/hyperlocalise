---
title: "Anthropic"
description: "Cấu hình Anthropic làm nhà cung cấp LLM cho việc tạo bản dịch."
---

## Giá trị nhà cung cấp

Sử dụng `anthropic` trong `llm.profiles.<name>.provider`.

## Ví dụ hồ sơ

```json
{
  "llm": {
    "profiles": {
      "default": {
        "provider": "anthropic",
        "model": "claude-sonnet-4-5",
        "prompt": "Translate from {{source}} to {{target}}:\n\n{{input}}"
      }
    }
  }
}
```

## Biến môi trường bắt buộc

<CodeGroup>
```bash Export
export ANTHROPIC_API_KEY="your-anthropic-api-key"
```

```bash .env.local
ANTHROPIC_API_KEY="your-anthropic-api-key"
```
</CodeGroup>

## Biến môi trường tùy chọn

`ANTHROPIC_BASE_URL` mặc định là `https://api.anthropic.com/v1`.
