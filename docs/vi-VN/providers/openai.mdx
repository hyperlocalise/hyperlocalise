---
title: "OpenAI"
description: "Cấu hình OpenAI làm nhà cung cấp LLM để tạo bản dịch."
---

## Giá trị của nhà cung cấp

Use `openai` in `llm.profiles.<name>.provider`.

## Ví dụ về hồ sơ

```json
{
  "llm": {
    "profiles": {
      "default": {
        "provider": "openai",
        "model": "gpt-5.2",
        "prompt": "Translate from {{source}} to {{target}}:\n\n{{input}}"
      }
    }
  }
}
```

## "Yêu cầu biến môi trường

<CodeGroup>
```bash Export
export OPENAI_API_KEY="your-openai-api-key"
```

```bash .env.local
OPENAI_API_KEY="your-openai-api-key"
```
</CodeGroup>
