---
title: "Bắt đầu nhanh"
description: "Thực hiện hyperlocalise toàn bộ quy trình trong khoảng 10 phút."
---

## Mục tiêu

Create an `i18n.jsonc`, run translation planning safely, generate outputs, and review status.

## 1. Khởi tạo cấu hình

```bash
hyperlocalise init
```

This writes `i18n.jsonc` in your current directory.

## 2. Cấu hình thông tin đăng nhập của nhà cung cấp

`hyperlocalise` automatically reads credentials from `.env` and `.env.local` in your current project directory. Exported shell variables still take precedence.

Ví dụ:

<CodeGroup>
```bash Export
export OPENAI_API_KEY="your-openai-api-key"
```

```bash .env.local
OPENAI_API_KEY="your-openai-api-key"
```
</CodeGroup>

OpenAI:

<CodeGroup>
```bash Export
export OPENAI_API_KEY="your-openai-api-key"
```

```bash .env.local
OPENAI_API_KEY="your-openai-api-key"
```
</CodeGroup>

Azure OpenAI:

<CodeGroup>
```bash Export
# Example: https://<resource>.openai.azure.com/openai/v1
export AZURE_OPENAI_BASE_URL="https://<resource>.openai.azure.com/openai/v1"
export AZURE_OPENAI_API_KEY="your-azure-openai-api-key"
```

```bash .env.local
# Example: https://<resource>.openai.azure.com/openai/v1
AZURE_OPENAI_BASE_URL="https://<resource>.openai.azure.com/openai/v1"
AZURE_OPENAI_API_KEY="your-azure-openai-api-key"
```
</CodeGroup>

Gemini:

<CodeGroup>
```bash Export
export GEMINI_BASE_URL="https://generativelanguage.googleapis.com/v1beta/openai"
export GEMINI_API_KEY="your-gemini-api-key"
```

```bash .env.local
GEMINI_BASE_URL="https://generativelanguage.googleapis.com/v1beta/openai"
GEMINI_API_KEY="your-gemini-api-key"
```
</CodeGroup>

Anthropic:

<CodeGroup>
```bash Export
export ANTHROPIC_BASE_URL="https://api.anthropic.com/v1"
export ANTHROPIC_API_KEY="your-anthropic-api-key"
```

```bash .env.local
ANTHROPIC_BASE_URL="https://api.anthropic.com/v1"
ANTHROPIC_API_KEY="your-anthropic-api-key"
```
</CodeGroup>

LM Studio:

<CodeGroup>
```bash Export
export LM_STUDIO_BASE_URL="http://127.0.0.1:1234/v1"
export LM_STUDIO_API_KEY="lm-studio"
```

```bash .env.local
LM_STUDIO_BASE_URL="http://127.0.0.1:1234/v1"
LM_STUDIO_API_KEY="lm-studio"
```
</CodeGroup>

Ollama:

<CodeGroup>
```bash Export
export OLLAMA_BASE_URL="http://127.0.0.1:11434/v1"
export OLLAMA_API_KEY="ollama"
```

```bash .env.local
OLLAMA_BASE_URL="http://127.0.0.1:11434/v1"
OLLAMA_API_KEY="ollama"
```
</CodeGroup>

Groq:

<CodeGroup>
```bash Export
export GROQ_BASE_URL="https://api.groq.com/openai/v1"
export GROQ_API_KEY="your-groq-api-key"
```

```bash .env.local
GROQ_BASE_URL="https://api.groq.com/openai/v1"
GROQ_API_KEY="your-groq-api-key"
```
</CodeGroup>

## 3. Thực hiện thử nghiệm trước

```bash
hyperlocalise run --dry-run
```

Kiểm tra HLMDPH_5D04BBA3F0B6_0, HLMDPH_8320BC5C3DC1_1, và các mục được liệt kê HLMDPH_CA7EA3026C7D_2.

## 4. Thực hiện tạo

```bash
hyperlocalise run
```

This writes translations and updates `.hyperlocalise.lock.json`. For details, see [Lockfile contract](/reference/lockfile-contract).

## 5. Xem lại trạng thái

```bash
hyperlocalise status --output csv
```

Use `needs_review` rows to identify entries that still need human curation.

## 6. Đánh giá chất lượng bản dịch khi cần thiết

```bash
hyperlocalise eval run --eval-set ./eval-set.json --output ./eval-report.json
```

Sử dụng công cụ này khi so sánh các lựa chọn yêu cầu/cấu hình/nhà cung cấp hoặc trước khi triển khai các bản cập nhật dịch thuật lớn.

## Tiếp

- Tune config in [i18n config](/configuration/i18n-config).
- Đồng bộ với hệ thống quản lý nội dung (TMS) của bạn bằng cách sử dụng [sync push](/commands/sync-push) và [sync pull](/commands/sync-pull).
- Check maturity expectations in the [stability matrix](/reference/stability-matrix).
