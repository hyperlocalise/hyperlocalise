---
title: "đồng bộ kéo xuống"
description: "Kéo các đầu ra của tác vụ Hyperlocalise đã hoàn tất vào các tệp cục bộ."
---

<Note>
Hợp đồng API việc làm công khai có thể phát triển trong khi các luồng công việc CI dần ổn định.
</Note>

## Cách sử dụng

```bash
hyperlocalise sync pull [--config <path>] [flags]
```

## Hành vi mặc định

`sync pull` mặc định áp dụng các đầu ra của tác vụ đã hoàn tất. Dùng `--dry-run` khi bạn muốn xem trước báo cáo mà không ghi tệp.

`sync pull` đọc một manifest công việc hoàn chỉnh, thăm dò API Hyperlocalise công khai theo ID công việc cho đến khi từng công việc đang chờ hoàn tất hoặc hết thời gian chờ đã cấu hình, tải xuống các tệp đầu ra thành công và ghi chúng vào các đường dẫn đích được phân giải bởi `i18n.yml`.

Manifest thường được truyền từ một công việc GitHub Actions `sync push` sang một công việc `sync pull` với `actions/upload-artifact` và `actions/download-artifact`.

## Cờ

- `--config`: đường dẫn cấu hình
- `--locale`: ngôn ngữ mục tiêu, có thể lặp lại
- `--manifest`: đường dẫn đến tệp manifest của các công việc
- `--timeout`: thời gian chờ tối đa, ví dụ `20m`
- `--dry-run`: chỉ xem trước
- `--output`: `text`, `json`, hoặc `markdown`

## Ví dụ

```bash
hyperlocalise sync pull --manifest .hyperlocalise/jobs.json --timeout 20m
```

```bash
hl pull --manifest .hyperlocalise/jobs.json
```

## Ghi chú

- `sync pull` từ chối các manifest có `complete: false`.
- Lỗi hết thời gian chờ bao gồm ID công việc đang chờ, đường dẫn nguồn, ngôn ngữ mục tiêu và trạng thái khi có.
- Các tệp đầu ra được ánh xạ theo ngôn ngữ sang các đường dẫn đích được ghi lại trong tệp kê khai.
- Các lệnh TMS cụ thể theo nhà cung cấp như `crowdin` và `phrase` giữ nguyên cấu hình và hành vi riêng của chúng.

## Xem thêm

- [chạy](/commands/run)
- [status](/commands/status)
- [sync push](/commands/sync-push)
- [Tự động hóa CI](/workflows/ci-automation)
