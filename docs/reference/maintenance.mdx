---
title: "Maintenance and release"
description: "Development workflow, quality checks, and release process."
---

## Quality workflow

Use the Makefile targets for local validation:

```bash
make fmt
make lint
make test
make precommit
```

Other common targets:

```bash
make check-build
make cover
make install
```

## Release process

Create and push a semantic version tag:

```bash
git tag v0.1.0
git push origin v0.1.0
```

Release automation and configuration:

- GitHub Actions workflow: `.github/workflows/release.yml`
- GoReleaser config: `.goreleaser.yml`

## Published artifacts

- macOS: `amd64`, `arm64`
- Linux: `amd64`, `arm64`
- Linux packages: `.deb`, `.rpm`
