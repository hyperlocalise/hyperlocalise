---
title: "init"
description: "在你的项目中创建或覆盖 i18n.jsonc 模板。"
---

## 用法

```bash
hyperlocalise init [--force]
```

## 它的作用是什么

- 写入 `i18n.jsonc` 来自内置的-在模板中
- 如果文件已存在，则会失败 (除非 `--force`)

## 标记

- `--force`：覆盖现有的 `i18n.jsonc`

## 典型流程

```bash
hyperlocalise init
hyperlocalise run --dry-run
```
