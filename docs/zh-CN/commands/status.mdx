---
title: "状态"
description: "报告按键/命名空间/语言环境的翻译状态，以 CSV 格式，并提供可选的交互式 TTY 仪表盘。"
---

## 使用方法

```bash
hyperlocalise status [--config <path>] [flags]
```

## 标志

- `--config`: path to config file
- `--locale`：目标区域过滤器（可重复）
- `--output`：输出格式 (`csv`)
- `--group`：按组名称过滤
- `--bucket`: 按桶名称过滤
- `-i`, `--interactive`：在TTY中渲染交互式仪表板（需要`--output csv`）

## 状态值

- `translated`：值不为空且未标记为草稿
- `needs_review`: provenance is `origin=llm` and `state=draft`
- `untranslated`: value is empty

## CSV 结构

`status --output csv` always emits this header:

```csv
key,namespace,locale,status,origin,state
```

列的含义：

- `key`：翻译键标识
- `namespace`：逻辑命名空间，用于按键分组
- `locale`: 目标区域
- `status`: 计算出的状态分类
- `origin`: value source (`llm`, `human`, or adapter-originated provenance)
- `state`: 工作流程状态 (例如 `draft` 或 经过筛选的等效状态)

## CI 兼容的标签映射（实验性）

使用此映射，当您的流程需要产品级别的标签时：

| CSV `status` | 准备好用于 CI 的标签 |
| --- | --- |
| `translated` | `ready` |
| `needs_review` | `needs review` |
| `untranslated` | `missing` |

## CI 闸口示例

当有任何条目时，则阻止：`missing`

```bash
hyperlocalise status --output csv > status.csv
awk -F, 'NR>1 && $4=="untranslated"{bad=1} END{exit bad}' status.csv
```

当`needs review` 的行数超过`20` 时：

```bash
hyperlocalise status --output csv > status.csv
awk -F, 'NR>1 && $4=="needs_review"{count++} END{exit(count>20)}' status.csv
```

## 示例

```bash
hyperlocalise status --locale es-ES --output csv
```

```bash
hyperlocalise status --group default --output csv -i
```

## 参见

- [运行](/commands/run)
- [eval](/commands/eval)
- [同步拉取](/commands/sync-pull)
- [同步推送](/commands/sync-push)
- [输出格式](/reference/output-formats)
