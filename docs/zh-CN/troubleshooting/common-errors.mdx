---
title: "常见错误"
description: "故障排除常用配置、设置、提供商和同步失败。"
---

## `i18n.jsonc already exists; use --force to overwrite`

原因：在已存在配置的情况下，尝试运行 `init`。

修复

```bash
hyperlocalise init --force
```

## `storage config is required`

原因：使用了不带顶层配置 [1mHLMDPH_C544220C52A1_1[0m [1mHLMDPH_40CAE40DAF05_0[0m 命令。

Fix: add `storage.adapter` and `storage.config` in `i18n.jsonc`.

## `unsupported output format`

原因：错误地使用了[1mHLMDPH_6573F78BA507_0[0m的值。

修复：

- `status`：仅限 `csv`
- `sync`： `text` 或 `json`

## `unsupported file extension` during `run`

原因：映射的源或目标文件使用了本地解析器不支持的扩展名。

修复：

- 使用以下任一选项：`.json`, `.arb`, `.xlf`, `.xliff`, `.po`, `.html`, `.md`, `.mdx`, `.strings`, `.stringsdict`, `.csv`
- verify bucket `from` and `to` paths resolve to the expected file names

## 提供商认证失败

原因：缺少/无效的 `Env token`。

修复：重新导出令牌并在同一 shell 会话中重新运行。
