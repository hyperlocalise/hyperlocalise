# CLI task guides

The user selected four proposed SEO/AEO topics: React Intl, changed-string
translation, GitHub Actions validation, and ICU troubleshooting. Use the agreed
page pattern: direct answer, prerequisites, runnable example, expected result,
limitations, and links to format and command references.

Add a local React Intl tutorial under `cli/guides`, an incremental translation
recipe under `cli/workflows`, and an ICU troubleshooting page. Expand the existing
GitHub Action guide at its current URL rather than creating a competing page.
Keep the existing Cloud React Intl tutorial focused on Cloud review.

Verify extraction and runtime catalog shapes, lockfile skip semantics, action
inputs and permissions, and ICU parity rules against source. Validate examples
with the installed CLI where possible without paid translation calls. Run Mintlify
link checks and preview the changed pages. Preserve other work in this checkout.
