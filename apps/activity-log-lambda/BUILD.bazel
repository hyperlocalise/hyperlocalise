load("@rules_go//go:def.bzl", "go_binary", "go_test")

go_binary(
    name = "activity_log_lambda",
    srcs = ["main.go"],
    importpath = "github.com/hyperlocalise/hyperlocalise/apps/activity-log-lambda",
    visibility = ["//visibility:public"],
    deps = [
        "//internal/activitylog",
        "@com_github_aws_aws_lambda_go//events",
        "@com_github_aws_aws_lambda_go//lambda",
        "@com_github_jackc_pgx_v5//pgxpool",
    ],
)

go_test(
    name = "activity_log_lambda_test",
    srcs = ["handler_test.go"],
    embed = [":activity_log_lambda"],
    deps = [
        "//internal/activitylog",
        "@com_github_aws_aws_lambda_go//events",
        "@com_github_jackc_pgx_v5//pgconn",
        "@com_github_stretchr_testify//require",
    ],
)
