# Agent Rules

## Documentation language scope

- Default to editing English docs only.
- Do not modify localized docs under `docs/vi-VN/` or `docs/zh-CN/` unless the user explicitly requests those language updates in the same task.
- When a docs change applies to all languages, update English first and ask before propagating to localized docs.
