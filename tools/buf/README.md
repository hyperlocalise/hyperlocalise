# Buf Tooling

This directory is reserved for Buf lint, breaking-change, and code-generation helpers that operate on [`api/proto`](../../api/proto).

Primary contract docs live in [`api/proto/README.md`](../../api/proto/README.md).

Current repo commands:

```bash
cd api/proto
buf lint
buf generate
```
